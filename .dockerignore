# VCS / IDE
.git
.gitignore
.github
.vscode
.idea

# Node junk
**/node_modules
npm-debug.log*
yarn-error.log*
pnpm-debug.log*
**/*.log

# Builds & caches (we build inside the image)
client/dist
server/.tmp
server/logs
coverage

# Local env/secrets (Copilot injects env; don't bake these)
.env
*.env
server/.env

# Docker / compose files not needed in image
docker-compose*.yml
Dockerfile.dev

# OS cruft
**/.DS_Store
Thumbs.db

# (Optional) repo-only stuff not needed at runtime
charts
assets

# IMPORTANT: do NOT ignore these — they must be sent to the build:
# - overlay/            (your overrides copied into the image)
# - client/src/**       (source to build)
# - server/**           (migrations, views, code)
# - certs/**            (if you vendor a PEM instead of curl in Dockerfile)
